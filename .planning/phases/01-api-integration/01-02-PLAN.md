---
phase: 01-api-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [TODO-01, TODO-02, TODO-03, TODO-04, TODO-05]
must_haves:
  truths:
    - "User can add new to-do task"
    - "User can view pending to-do tasks"
    - "User can edit to-do task"
    - "User can mark task as complete"
    - "User can delete to-do task"
  artifacts:
    - path: "ViewModels/AddToDoViewModel.cs"
      provides: "Add to-do functionality"
    - path: "ViewModels/ToDoListViewModel.cs"
      provides: "Get to-do functionality"
    - path: "ViewModels/EditToDoViewModel.cs"
      provides: "Update, status change, delete functionality"
  key_links:
    - from: "AddToDoViewModel.SaveAsync"
      to: "DatabaseService.InsertToDoAsync"
      via: "await AppServices.Database.InsertToDoAsync"
    - from: "ToDoListViewModel.LoadItemsAsync"
      to: "DatabaseService.GetToDoItemsAsync"
      via: "await AppServices.Database.GetToDoItemsAsync"
    - from: "EditToDoViewModel.SaveAsync"
      to: "DatabaseService.UpdateToDoAsync"
      via: "await AppServices.Database.UpdateToDoAsync"
---

<objective>
Verify To Do CRUD APIs (Add, Get, Update, Change Status, Delete) are correctly integrated.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

From Services/DatabaseService.cs:
```csharp
public async Task<List<ToDoClass>> GetToDoItemsAsync(int userId, string status)
public async Task<int> InsertToDoAsync(ToDoClass item)
public async Task<int> UpdateToDoAsync(ToDoClass item)
public async Task<int> DeleteToDoAsync(ToDoClass item)
```
</context>

<tasks>

<task type="auto">
  <name>task 1: Verify Add To Do API</name>
  <files>ViewModels/AddToDoViewModel.cs, Services/DatabaseService.cs</files>
  <action>
Verify AddToDoViewModel.SaveAsync:
1. Validates name required (non-empty)
2. Creates ToDoClass with status="pending"
3. Sets user_id from AuthService.CurrentUser
4. Calls DatabaseService.InsertToDoAsync
5. Navigates back on success
</action>
  <verify>
<automated>grep -l "InsertToDoAsync\|status.*pending\|user_id" ViewModels/AddToDoViewModel.cs</automated>
</verify>
  <done>Add: name provided → created with pending status</done>
</task>

<task type="auto">
<parameter name="task 2: Verify Get To Do API</name>
  <files>ViewModels/ToDoListViewModel.cs, Services/DatabaseService.cs</files>
  <action>
Verify ToDoListViewModel.LoadItemsAsync:
1. Gets user_id from AuthService.CurrentUser
2. Calls DatabaseService.GetToDoItemsAsync with "pending"
3. Populates ObservableCollection
</action>
  <verify>
<automated>grep -l "GetToDoItemsAsync\|ObservableCollection" ViewModels/ToDoListViewModel.cs</automated>
</verify>
  <done>Get: loads pending items into collection</done>
</task>

<task type="auto">
<parameter name>task 3: Verify Update, Status Change, Delete APIs</name>
  <files>ViewModels/EditToDoViewModel.cs, Services/DatabaseService.cs</files>
  <action>
Verify EditToDoViewModel:
1. SaveAsync: Updates item_name, item_description, calls UpdateToDoAsync
2. MarkCompleteAsync: Sets status="completed", calls UpdateToDoAsync
3. DeleteAsync: Calls DatabaseService.DeleteToDoAsync (with confirmation)
</action>
  <verify>
<automated>grep -l "UpdateToDoAsync\|DeleteToDoAsync\|status.*completed" ViewModels/EditToDoViewModel.cs</automated>
</verify>
  <done>Update: modifies fields. Status: toggles pending/completed. Delete: removes item</done>
</task>

</tasks>

<verification>
- AddToDoViewModel uses DatabaseService.InsertToDoAsync
- ToDoListViewModel uses DatabaseService.GetToDoItemsAsync
- EditToDoViewModel uses DatabaseService.UpdateToDoAsync and DeleteToDoAsync
- Status changes between "pending" and "completed"
</verification>

<success_criteria>
- [ ] User can add new to-do task
- [ ] User can view pending to-do tasks
- [ ] User can edit to-do task details
- [ ] User can mark task as completed
- [ ] User can delete to-do task
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-integration/01-02-SUMMARY.md`
</output>