---
phase: 01-api-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [AUTH-01, AUTH-02]
must_haves:
  truths:
    - "User can sign in with valid credentials"
    - "User can register a new account"
    - "Invalid credentials show error message"
  artifacts:
    - path: "ViewModels/LoginViewModel.cs"
      provides: "Sign in functionality"
    - path: "ViewModels/RegisterViewModel.cs"
      provides: "Sign up functionality"
    - path: "Services/DatabaseService.cs"
      provides: "User CRUD operations"
  key_links:
    - from: "LoginViewModel.LoginAsync"
      to: "DatabaseService.GetUserAsync"
      via: "await AppServices.Database.GetUserAsync"
    - from: "RegisterViewModel.RegisterAsync"
      to: "DatabaseService.CreateUserAsync"
      via: "await AppServices.Database.CreateUserAsync"
---

<objective>
Verify authentication APIs (Sign in, Sign Up) are correctly integrated.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

From Services/DatabaseService.cs:
```csharp
public async Task<UserClass?> GetUserAsync(string email)
public async Task<int> CreateUserAsync(UserClass user)
```

From Services/AuthService.cs:
```csharp
public UserClass? CurrentUser { get; private set; }
public bool IsLoggedIn => CurrentUser is not null;
public void SetUser(UserClass user)
```
</context>

<tasks>

<task type="auto">
  <name>task 1: Verify Sign In API integration</name>
  <files>ViewModels/LoginViewModel.cs, Services/DatabaseService.cs, Services/AuthService.cs</files>
  <action>
Verify LoginViewModel.LoginAsync:
1. Calls DatabaseService.GetUserAsync with email
2. Compares password hash using HashHelper.Hash
3. Sets user in AuthService on success
4. Navigates to AppShell on success
5. Shows error alert on failure
</action>
  <verify>
<automated>grep -l "GetUserAsync\|HashHelper.Hash\|AuthService.Instance.SetUser" ViewModels/LoginViewModel.cs</automated>
</verify>
  <done>Login flow: valid creds → AppShell, invalid creds → error alert</done>
</task>

<task type="auto">
  <name>task 2: Verify Sign Up API integration</name>
  <files>ViewModels/RegisterViewModel.cs, Services/DatabaseService.cs, Models/UserClass.cs</files>
  <action>
Verify RegisterViewModel.RegisterAsync:
1. Validates all fields required
2. Validates password match
3. Validates password min 6 chars
4. Checks existing user (duplicate email)
5. Creates new UserClass with hashed password
6. Calls DatabaseService.CreateUserAsync
7. Navigates back on success
</action>
  <verify>
<automated>grep -l "CreateUserAsync\|password_hash\|HashHelper.Hash" ViewModels/RegisterViewModel.cs</automated>
</verify>
  <done>Register flow: new user → created, duplicate → error, mismatch → error</done>
</task>

</tasks>

<verification>
- LoginViewModel uses DatabaseService.GetUserAsync
- RegisterViewModel uses DatabaseService.CreateUserAsync  
- Both use HashHelper for password hashing
- AuthService tracks current user
</verification>

<success_criteria>
- [ ] Sign in works with valid credentials
- [ ] Sign in shows error with invalid credentials
- [ ] Sign up creates new user account
- [ ] Sign up validates duplicate email
- [ ] Sign up validates password match
</success_criteria>

<output>
After completion, create `.planning/phases/01-api-integration/01-01-SUMMARY.md`
</output>